import template from './shopList.html';
import controller from './shopList.controller';
import './shopList.scss';

let shopListComponent = {
  restrict: 'E',
  bindings: {
  },
  template,
  controller
};

export default shopListComponent;

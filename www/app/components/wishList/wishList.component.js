import template from './wishList.html';
import controller from './wishList.controller';
import './wishList.scss';

let wishListComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default wishListComponent;

import wishListButtonComponent from './wishListButton.component';

let wishListButtonModule = angular.module('wishListButton', [
  'ionic'
])

.component('wishListButton', wishListButtonComponent)

.name;

export default wishListButtonModule;

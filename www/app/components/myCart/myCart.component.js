import template from './myCart.html';
import controller from './myCart.controller';
import './myCart.scss';

let myCartComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default myCartComponent;
